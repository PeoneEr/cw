<%= simple_form_for @employee do |f| %>
  <%= f.input :fio %>
  <%= f.input :birthday, as: :date, start_year: Date.today.year - 50, end_year: Date.today.year - 12, order: [:day, :month, :year] %>
  <%= f.submit "Добавить" %>
<% end %>
